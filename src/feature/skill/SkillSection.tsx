import { Button } from '@/components/ui/index'

const SkillSection = () => {
  return (
    <div>
      기술영역
      <Button className="text-black">버튼</Button>
    </div>
  )
}

export default SkillSection
